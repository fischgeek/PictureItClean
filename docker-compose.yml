services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ${DATA_PATH_ON_HOST:-./data}:/data
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
    restart: unless-stopped
